Import `PdfEditor.postman_collection.json` from this folder into Postman.

After running the `Login Owner` request, copy the returned `accessToken` into the `accessToken` collection variable, then run the document, annotation, and compression requests.
